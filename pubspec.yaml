name: c2sanilist
description: Anime browser built with AniList APIs

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Architecture
  flutter_bloc: ^7.0.0

  # Networking
  http: ^0.13.3

  # Miscellaneous
  meta: ^1.3.0
  equatable: ^2.0.0
  url_launcher: ^6.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.0

flutter:
  uses-material-design: true
